<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- Android 9 이하에서만 필요 -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <!-- 카메라 하드웨어는 선택 사항 -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <!-- (선택) 전/후면 아무 카메라나 허용 표시 -->
    <uses-feature
        android:name="android.hardware.camera.any"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:usesCleartextTraffic="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:targetApi="31">
        <activity android:name=".ui.start.AgreementActivity" />
        <!-- activity android:name=".ui.StartActivity" -->
        <activity android:name=".ui.start.SignupActivity" />
        <activity android:name=".ui.login.LoginActivity" />
        <activity android:name=".ui.main.MainActivity" />
        <activity android:name=".ui.start.unused_SelectUserTypeActivity" />
        <activity android:name=".ui.main.mypage.MyPageActivity" />
        <activity android:name=".ui.login.find.FindEmailActivity" />
        <activity android:name=".ui.login.find.FindEmailResultActivity" />
        <activity android:name=".ui.login.find.FindPasswordActivity" />
        <activity android:name=".ui.login.find.FindPasswordCodeActivity" />
        <activity android:name=".ui.main.mypage.ChangePasswordCurrentActivity" />
        <activity android:name=".ui.main.mypage.ChangePasswordNewActivity" />
        <activity android:name=".ui.main.mypage.DocumentsActivity" />
        <activity android:name=".ui.main.translation.TranslationDocActivity" />
        <activity android:name=".ui.main.translation.TranslationDocImagesActivity" />
        <activity android:name=".ui.main.translation.camera.PhotoCaptureGuideActivity" />
        <activity android:name=".ui.main.translation.camera.CameraCaptureActivity" />
        <activity android:name="com.example.lingo.ui.main.translation.TranslationFamilyResultActivity" />
        <activity android:name=".ui.main.translation.camera.PhotoReviewActivity" />
        <activity android:name=".ui.main.translation.TranslationInProgressActivity" />
        <activity android:name=".ui.main.translation.TranslationResultActivity" />
        <activity android:name=".ui.main.translation.document.DocumentGeneratingActivity" />
        <activity android:name=".ui.main.translation.document.DocumentPreviewActivity" />
        <activity
            android:name=".ui.login.find.FindPasswordResultActivity"
            android:exported="false" />
        <activity
            android:name=".ui.start.StartActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/filepaths" />
        </provider>
    </application>

</manifest>